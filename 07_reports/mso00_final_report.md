# MSO-00 final report

## Terminal status

`MSO_PROJECT_AND_PRELEARNING_MULTISCALE_IDENTIFIABILITY_CONTRACT_FROZEN`

The status is granted because DDO/PIO provenance is bounded and recorded, ARC scientific import is zero, the learning object and SS/MS contrast are frozen, the scale ladder remains a prospective candidate, the firewall and evidence roles are explicit, literature threats are registered, and all H-MSO-01 metrics/gates were fixed without fresh target access. No scientific experiment, target evaluation, training, optimizer, integration, solver-in-loop, rollout, or sealed-test access occurred.

## 1. What does the new project study?

SPH-MSO studies whether the response of a fixed, deployment-available SPH semidiscrete operator to changes in compact-support scale carries missing information about the already-defined instantaneous spatial discretization defect. The first test is prelearning identifiability, not neural performance.

## 2. Scientific difference from DDO

DDO tested deployment-observable instantaneous defect identifiability under its registered single-scale representations and closed that tested route. MSO keeps DDO's target exactly unchanged but introduces a prospective paired intervention: expose the same operator evaluated on the same state at several support scales. DDO H3 outcomes provide historical problem scale only and are not MSO results or the SS control.

## 3. Scientific difference from PIO

PIO studied and qualified/failed broader structure-preserving operators, neural corrections, optimizer paths, training, and full-solver routes. MSO-00 contains no model or dynamics. It can reuse only provenance-audited static interfaces and governance concepts. PIO architecture, training, validation, and failure/success outcomes do not answer H-MSO-01.

## 4. Is ARC completely excluded from the scientific evidence chain?

Yes. ARC was not read or used. No ARC scientific result, parameter, status, training evidence, conclusion, or code was imported. Candidate PIO Stage01C files have an available PIO Git origin and match DDO's audited copies byte-for-byte; no explicit ARC path/content/history-subject conflict was found. Any later evidence of ARC derivation triggers quarantine and `PROVENANCE_CONFLICT` rather than silent use.

## 5. Five most dangerous novelty overlaps

1. Woodward et al. (2023), parameterized SPH kernels and learned Lagrangian turbulence hierarchy — `HIGH`.
2. Winchenbach & Thuerey (2024), symmetric basis continuous convolutions for Lagrangian fluids — `HIGH`.
3. Prantl et al. (2022), hard momentum-conserving hierarchical particle networks — `HIGH`.
4. Tian et al. (2023), physics-informed Lagrangian LES with generalized SPH — `HIGH`.
5. Wang et al. (2026), FluidFormer local-continuous-convolution/global-attention particle simulator — `HIGH`.

Consequently MSO makes no first-attention, first-Transformer, first-multiscale-neural-CFD, first-learned-kernel, first-mesh-free-operator, or first-differentiable-SPH claim.

## 6. Formal falsifiable H-MSO-01 proposition

On a fully fresh, balanced, lineage-held-out atlas, with the frozen DDO defect and qualified SPH operator, the MS representation must pass every absolute gate and, relative to the paired SS control, reduce DNN p90 and conditional-variance point estimates by at least 20% (simultaneous UCB shows at least 10%) and non-neural oracle NRMSE by at least 15% (simultaneous UCB shows at least 5%) for each of the three primary components. The global hypothesis passes only when all three components pass.

## 7. What is the only SS/MS variable that changes?

`representation` only. SS exposes the frozen base deployment family and \(\mathcal L_h(q_h)\). MS exposes the same base plus qualified \(\mathcal L_{\lambda h}(q_h)\), baseline differences, log-scale divided differences, and target-blind topology summaries. Cases, particles, physics, target, fields, kernel formula, precision, splits, oracle families, bootstrap draws, and aggregation are identical.

## 8. Which quantities are deployment-available?

Particle positions (used as minimum-image relative geometry, not default absolute coordinates), masses/volumes, density, local/relative velocity information, support \(h\), particle spacing/numerical resolution parameters, EOS constants, viscosity, domain/kernel identifiers, neighbor/kernel moment summaries, the frozen base operator outputs, and—after MSO-01 qualification—the same operator's candidate-scale values and algebraic response summaries. Normalization may use development-fold deployment observables only.

## 9. Which quantities are absolutely prohibited as inputs?

All reference fields (`q_ref`, `rho_ref`, `v_ref`, `a_ref`), continuum/reference operator values, \(d_h^*\), all `target_ref__*`/`target__*` fields, reference-minus-low-cost proxies, manufactured-field identity, hidden lineage labels, design-only field information, target-derived scales/features/normalization/neighborhoods, and high-resolution SPH as truth. Absolute global coordinates are prohibited by default.

## 10. Is MSO-01 authorized?

Yes, but only as a target-blind numerical-qualification stage. It is not authorized by this report to execute automatically; a separate instruction is required.

## 11. What may MSO-01 do?

MSO-01 may bind audited static source identities; verify support/domain validity; deterministic, reciprocal, deduplicated, nested graphs; minimum neighbor support; finite values; uniform-velocity continuity/viscosity identities; reciprocal pressure/viscosity force and dissipation checks; exact \(\lambda=1\) equivalence; topology-change diagnostics; response-to-repeat-noise ratios; and optional AD/FD interface consistency with topology caveats. It may accept/reject the frozen candidate ladder using those checks only.

MSO-01 may not generate/read defects or continuum references, inspect H3 outcomes, select scales/features from targets, train, optimize, integrate, roll out, use a sealed test, or replace a failed multiplier after target access.

## 12. Did any new scientific compute, target access, or training occur?

No. `NEW_SCIENTIFIC_TARGET_EVALUATION=false`, `NEURAL_MODEL_INSTANTIATION=false`, `NEURAL_TRAINING=false`, `OPTIMIZER_EXECUTION=false`, `TIME_INTEGRATION_EXPERIMENT=false`, `SOLVER_IN_LOOP=false`, `ROLLOUT=false`, and `SEALED_TEST_ACCESS=false`. Work was limited to read-only repository/interface/provenance audit, literature metadata verification, and prospective contract drafting.

## Freeze checklist

- DDO/PIO provenance clear: **YES**.
- ARC scientific evidence import: **ZERO**.
- First learning object frozen: **YES**.
- SS/MS causal contrast frozen: **YES**.
- Scale ladder prospective candidate: **YES**.
- Observable/reference-target firewall: **YES**.
- Literature threats registered: **YES**.
- Prospective H-MSO-01 metrics/gates: **YES**.
- Fresh target outcome accessed: **NO**.
- Training/optimizer/solver/rollout executed: **NO**.

Stop after MSO-00. Do not auto-execute MSO-01.
